Web Lego - Конструктор одностраничных сайтов
Web Lego - это интуитивный конструктор для создания одностраничных сайтов без знания программирования. Создавайте уникальные лендинги, перетаскивая блоки и настраивая их параметры.

🚀 Возможности
📝 Создание одностраничных сайтов через визуальный редактор
🎨 Перетаскивание блоков (текст, заголовки, изображения, видео, кнопки, слайдеры)
🖼️ Загрузка изображений или использование URL
🎯 Настройка цветов, размеров и позиций блоков
📱 Адаптивный дизайн
👤 Регистрация и авторизация пользователей
💾 Сохранение и управление несколькими сайтами
📋 Требования
Python 3.11+
Docker и Docker Compose (для запуска через Docker)
Или PostgreSQL (для запуска без Docker)
🐳 Запуск с Docker (Рекомендуется)
Клонируйте репозиторий:
git clone <repository-url>
cd nocode/web_lego
Запустите проект:
docker-compose up --build
Выполните миграции (в новом терминале):
docker-compose exec web python manage.py migrate
Создайте суперпользователя (опционально):
docker-compose exec web python manage.py createsuperuser
Откройте браузер и перейдите по адресу: http://localhost:8000
💻 Запуск без Docker
Создайте виртуальное окружение:
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
Установите зависимости:
pip install -r requirements.txt
Настройте базу данных в settings.py (по умолчанию используется SQLite)

Выполните миграции:

python manage.py migrate
Создайте суперпользователя:
python manage.py createsuperuser
Запустите сервер:
python manage.py runserver
Откройте браузер: http://localhost:8000
📁 Структура проекта
web_lego/
├── base/                 # Основное приложение
│   ├── models.py        # Модели данных (Website, Block)
│   ├── views.py         # Представления
│   ├── urls.py          # URL маршруты
│   ├── templates/       # HTML шаблоны
│   ├── static/          # Статические файлы (CSS, JS)
│   └── templatetags/    # Кастомные теги шаблонов
├── web_lego/            # Настройки проекта
│   ├── settings.py      # Настройки Django
│   └── urls.py          # Главные URL маршруты
├── manage.py            # Управляющий скрипт Django
├── requirements.txt     # Python зависимости
├── Dockerfile           # Конфигурация Docker
└── docker-compose.yml   # Docker Compose конфигурация
🎯 Использование
Регистрация: Создайте аккаунт на главной странице
Создание сайта: Перейдите в Dashboard и нажмите "Создать сайт"
Редактирование:
Перетащите блоки из панели слева на canvas
Нажмите на блок для редактирования
Измените размер, перетаскивая углы блока
Настройте параметры в панели справа
Просмотр: Нажмите "Открыть сайт" для просмотра результата
🛠️ Технологии
Backend: Django 4.2+
Frontend: HTML, CSS, JavaScript
База данных: SQLite (по умолчанию) / PostgreSQL (в Docker)
Контейнеризация: Docker, Docker Compose
📝 Команды Docker
# Запуск
docker-compose up

# Запуск в фоне
docker-compose up -d

# Остановка
docker-compose down

# Пересборка
docker-compose up --build

# Выполнение команд Django
docker-compose exec web python manage.py <command>

# Просмотр логов
docker-compose logs -f web
🔧 Настройка
Переменные окружения
Для production рекомендуется использовать переменные окружения:

SECRET_KEY - секретный ключ Django
DEBUG - режим отладки (False для production)
ALLOWED_HOSTS - разрешенные хосты
DATABASE_URL - URL базы данных (если используется PostgreSQL)
Использование PostgreSQL в Docker
Проект уже настроен для работы с PostgreSQL в Docker Compose. База данных автоматически создается при первом запуске.


👨‍💻 Разработка
Для разработки рекомендуется:

Использовать виртуальное окружение
Установить зависимости: pip install -r requirements.txt
Запустить миграции: python manage.py migrate
Запустить сервер разработки: python manage.py runserver
🐛 Известные проблемы
При использовании SQLite в production могут возникнуть проблемы с параллельными запросами
Рекомендуется использовать PostgreSQL для production
